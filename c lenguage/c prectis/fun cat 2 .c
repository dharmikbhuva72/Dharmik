#include<stdio.h>

void isprime(int no);

void main()
{
	int prime;
	
	printf("\n ENTER NO TO FIND PRIME OR NOT :");
	scanf("%d",&prime);
	isprime(prime); 
}
void isprime(int no)
{
	int i ,c = 0;
	
	for(no %d == 0)
	{
		c++
	}
	if(c== 2)
	{
		printf("\n%d is not prime",no);
		
	}
	else
	{
		printf("\n%d is not prime ",no);
		
	}
}